services:
  postgres:
    image: postgres:16-alpine
    container_name: jupiter-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-jupiter}
    volumes:
    - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -h 127.0.0.1 -U ${DB_USER:-postgres} -d ${DB_NAME:-jupiter}
      interval: 10s
      timeout: 5s
      retries: 5
    network_mode: host
  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: jupiter-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS:-guest}
    volumes:
    - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test:
      - CMD
      - rabbitmq-diagnostics
      - ping
      interval: 10s
      timeout: 5s
      retries: 5
    network_mode: host
  minio:
    image: quay.io/minio/minio:latest
    container_name: jupiter-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    volumes:
    - minio_data:/data
    healthcheck:
      test:
      - CMD
      - mc
      - ready
      - local
      interval: 10s
      timeout: 5s
      retries: 5
    network_mode: host
  migrate:
    build:
      context: ./infra/migrations
      dockerfile: Dockerfile
    image: jupiter-migrate
    container_name: jupiter-migrate
    working_dir: /migrations
    volumes:
    - ./infra/migrations:/migrations
    environment:
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@127.0.0.1:5432/${DB_NAME:-jupiter}
    depends_on:
      postgres:
        condition: service_healthy
    restart: 'no'
    network_mode: host
  gateway:
    build:
      context: ./api-gateway
      dockerfile: Dockerfile
    container_name: jupiter-gateway
    restart: unless-stopped
    environment:
      PORT: 8080
      DB_HOST: 127.0.0.1
      DB_PORT: 5432
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_NAME: ${DB_NAME:-jupiter}
      JWT_SECRET: ${JWT_SECRET:-change_me_in_production}
      JWT_EXPIRY_MINUTES: ${JWT_EXPIRY_MINUTES:-15}
      JWT_REFRESH_EXPIRY_HOURS: ${JWT_REFRESH_EXPIRY_HOURS:-168}
      RABBITMQ_URL: amqp://${RABBITMQ_USER:-guest}:${RABBITMQ_PASS:-guest}@127.0.0.1:5672/
      MINIO_ENDPOINT: 127.0.0.1:9000
      MINIO_PUBLIC_ENDPOINT: 127.0.0.1:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      MINIO_BUCKET: ${MINIO_BUCKET:-jupiter-videos}
      MINIO_USE_SSL: 'false'
      MINIO_PUBLIC_USE_SSL: 'false'
      CORS_ALLOW_ORIGIN: ${CORS_ALLOW_ORIGIN:-http://localhost:8090}
      GROQ_API_KEY: ${GROQ_API_KEY}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      ELEVENLABS_API_KEY: ${ELEVENLABS_API_KEY:-}
      ELEVENLABS_VOICE_ID: ${ELEVENLABS_VOICE_ID:-EXAVITQu4vr4xnSDxMaL}
      ELEVENLABS_MODEL_ID: ${ELEVENLABS_MODEL_ID:-eleven_multilingual_v2}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:-}
      STRIPE_PUBLISHABLE_KEY: ${STRIPE_PUBLISHABLE_KEY:-}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET:-}
      STRIPE_PRICE_STARTER: ${STRIPE_PRICE_STARTER:-}
      STRIPE_PRICE_GROWTH: ${STRIPE_PRICE_GROWTH:-}
      STRIPE_PRICE_PRO: ${STRIPE_PRICE_PRO:-}
      STRIPE_PRICE_SCALE: ${STRIPE_PRICE_SCALE:-}
      STRIPE_PRICE_TOPUP_S: ${STRIPE_PRICE_TOPUP_S:-}
      STRIPE_PRICE_TOPUP_M: ${STRIPE_PRICE_TOPUP_M:-}
      STRIPE_PRICE_TOPUP_L: ${STRIPE_PRICE_TOPUP_L:-}
      BILLING_SUCCESS_URL: ${BILLING_SUCCESS_URL:-http://localhost:5173/billing-success.html}
      BILLING_CANCEL_URL: ${BILLING_CANCEL_URL:-http://localhost:5173/billing-cancel.html}
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      minio:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO- http://127.0.0.1:8080/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 5
    network_mode: host
  worker-pose:
    build:
      context: ./ai-workers/pose
      dockerfile: Dockerfile
    container_name: jupiter-worker-pose
    restart: unless-stopped
    environment:
      RABBITMQ_HOST: 127.0.0.1
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASS: ${RABBITMQ_PASS:-guest}
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@127.0.0.1:5432/${DB_NAME:-jupiter}
      S3_ENDPOINT_URL: http://127.0.0.1:9000
      AWS_ACCESS_KEY_ID: ${MINIO_ACCESS_KEY:-minioadmin}
      AWS_SECRET_ACCESS_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      AWS_REGION: us-east-1
      POSE_JOBS_QUEUE: pose.jobs
      FEATURES_RESULTS_QUEUE: features.results
      POSE_SEGMENT_SECONDS: 5
      POSE_MAX_VIDEO_SECONDS: 600
    depends_on:
      rabbitmq:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test:
      - CMD-SHELL
      - python -c "import os,sys; sys.exit(0 if any('pose_worker' in open('/proc/'+p+'/cmdline','rb').read().decode('latin1')
        for p in os.listdir('/proc') if p.isdigit()) else 1)"
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
    network_mode: host
  worker-whisper:
    build:
      context: ./ai-workers/whisper
      dockerfile: Dockerfile
    container_name: jupiter-worker-whisper
    restart: unless-stopped
    environment:
      RABBITMQ_HOST: 127.0.0.1
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASS: ${RABBITMQ_PASS:-guest}
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@127.0.0.1:5432/${DB_NAME:-jupiter}
      S3_ENDPOINT_URL: http://127.0.0.1:9000
      AWS_ACCESS_KEY_ID: ${MINIO_ACCESS_KEY:-minioadmin}
      AWS_SECRET_ACCESS_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      AWS_REGION: us-east-1
      WHISPER_JOBS_QUEUE: whisper.jobs
      FEATURES_RESULTS_QUEUE: features.results
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      GROQ_API_KEY: ${GROQ_API_KEY}
      WHISPER_MODEL: whisper-1
      GROQ_WHISPER_MODEL: whisper-large-v3
      WHISPER_MAX_VIDEO_SECONDS: 480
      WHISPER_CHUNK_SECONDS: 120
    depends_on:
      rabbitmq:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test:
      - CMD-SHELL
      - python -c "import os,sys; sys.exit(0 if any('whisper_worker' in open('/proc/'+p+'/cmdline','rb').read().decode('latin1')
        for p in os.listdir('/proc') if p.isdigit()) else 1)"
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    network_mode: host
  worker-prosody:
    build:
      context: ./ai-workers
      dockerfile: prosody/Dockerfile
    container_name: jupiter-worker-prosody
    restart: unless-stopped
    environment:
      RABBITMQ_HOST: 127.0.0.1
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASS: ${RABBITMQ_PASS:-guest}
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@127.0.0.1:5432/${DB_NAME:-jupiter}
      PROSODY_QUEUE: prosody.jobs
      S3_ENDPOINT_URL: http://127.0.0.1:9000
      AWS_ACCESS_KEY_ID: ${MINIO_ACCESS_KEY:-minioadmin}
      AWS_SECRET_ACCESS_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      AWS_REGION: us-east-1
    depends_on:
      rabbitmq:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO- http://127.0.0.1:8001/health || exit 1
      interval: 15s
      timeout: 5s
      retries: 3
    network_mode: host
  worker-scoring:
    build:
      context: ./ai-workers
      dockerfile: scoring/Dockerfile
    container_name: jupiter-worker-scoring
    restart: unless-stopped
    environment:
      RABBITMQ_HOST: 127.0.0.1
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASS: ${RABBITMQ_PASS:-guest}
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@127.0.0.1:5432/${DB_NAME:-jupiter}
      DB_HOST: 127.0.0.1
      DB_PORT: 5432
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_NAME: ${DB_NAME:-jupiter}
      SCORING_QUEUE: scoring.jobs
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      GROQ_API_KEY: ${GROQ_API_KEY}
    depends_on:
      rabbitmq:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO- http://127.0.0.1:8002/health || exit 1
      interval: 15s
      timeout: 5s
      retries: 3
    network_mode: host
  frontend:
    image: nginx:alpine
    container_name: jupiter-frontend
    restart: unless-stopped
    volumes:
    - ./frontend:/usr/share/nginx/html:ro
    - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      gateway:
        condition: service_healthy
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO- http://127.0.0.1:80/seller || exit 1
      interval: 15s
      timeout: 5s
      retries: 5
    network_mode: host
volumes:
  postgres_data: null
  rabbitmq_data: null
  minio_data: null
